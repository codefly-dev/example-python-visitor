apiVersion: v1
kind: Service
metadata:
  name: "write-cache"
  namespace: "python-visitors-backend"
spec:
  selector:
    app: "cache-write"
  ports:
    - protocol: TCP
      name: tcp-port
      port: 7070
      targetPort: 6379
