apiVersion: v1
kind: ConfigMap
metadata:
  name: cache-config
  namespace: "python-visitors-localkube"
data:
  redis.conf: |
    # Redis configuration file
    bind 0.0.0.0
    protected-mode no
    # Other configurations
