apiVersion: v1
kind: Service
metadata:
  name: clicker
  namespace: python-visitors-backend-aws
spec:
  selector:
    app: clicker
  ports:
    - protocol: TCP
      name: http-port
      port: 8080
      targetPort: 8080
